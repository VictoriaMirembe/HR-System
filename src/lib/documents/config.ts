// PDF-only, per the acceptance criteria ("previewable in-browser and
// downloadable as PDF") — policies/handbooks/manuals are what this
// repository is for, not a general file store.
export const ALLOWED_MIME_TYPE = "application/pdf";
export const MAX_DOCUMENT_SIZE_BYTES = 20 * 1024 * 1024; // 20 MB
